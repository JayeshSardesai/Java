class Solution {
    public int maxFrequency(int[] nums, int k) {
        Arrays.sort(nums);
        int freq1=1;
        int freq2=0;
        int i=0;
        int max=1;
        int l=k;
        while(i<arr.length{
            if(max<)
            int j=0;
            while(j<k){
                if(nums.contains(nums[i]+(j+1))){
                    max++;

                    break;
                }
            }
            i++;
        }
    }
}
